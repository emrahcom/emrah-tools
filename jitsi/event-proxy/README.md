Install [deno](https://deno.land/) first.

#### running (prod)

```bash
deno run --allow-net index.ts
```

#### running (dev)

```bash
deno run --allow-net --watch index.ts
```

#### checking

```bash
deno fmt --check
deno lint
```
